import Team from "../entity/Team"
import User from "../entity/User"

/**
 * Adds a user to a team
 */
export const addUserToTeam = (user: User, team: Team) => {

}

/**
 * Removes a user from a team
 */
export const removeUserFromTeam = (user: User, team: Team) => {

}

/**
 * Returns all the teams the user is in
 */
export const getUserTeams = (user:User) => {
    
}

/**
 * Returns all the users inside the given team
 */
export const getTeamUsers = (team: Team) => {
    
}